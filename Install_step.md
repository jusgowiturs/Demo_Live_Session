# Installation Steps

## 1. Install Icarus Verilog

Update package list:

```bash
sudo apt update
sudo apt install iverilog

```
Check installation:
```bash
iverilog -V
```
## 2. Install GTKWave

Install waveform viewer:
```
sudo apt install gtkwave
```
Check installation:
```
gtkwave --version
```
